services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
